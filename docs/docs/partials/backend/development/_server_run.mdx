import CodeBlock from '@theme/CodeBlock';

When you run the server with `start` you need to be sure `key` is properly set.
It can be either defined in the `.env` file or passed to the command as `--key` parameter

<CodeBlock language="bash">
  yarn {props.platformPrefix}:backend start --key my-strong-key
</CodeBlock>
