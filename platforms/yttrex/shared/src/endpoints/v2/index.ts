import Metadata from './metadata.endpoints';
import Public from './public.endpoints';
import Experiments from './experiments.endpoints';

const endpoints = {
  Public,
  Metadata,
  Experiments,
};

export default endpoints;
