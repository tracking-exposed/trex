import { join } from 'path';

export const massPath = join(__dirname, '../mass');
export const parsedPath = join(__dirname, '../parsed');
export const maxParallelWorkers = 10;
export const MONGO_URL = 'mongodb://localhost:27017/';
