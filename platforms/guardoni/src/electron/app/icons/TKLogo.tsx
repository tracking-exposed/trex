import * as React from 'react';
import ytLogo from '../../../../assets/tk-logo.png';

const TKLogo: React.FC = () => {
  return <img style={{ height: 30, width: 'auto' }} src={ytLogo} />;
};

export default TKLogo;
