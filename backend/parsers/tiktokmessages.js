const _ = require('lodash');
const debug = require('debug')('parser:messages');

/* by 'messages' we mean the text returned as part of an error,
 * or something like 'not found', localized */

function search(envelop, previous) {

    return null;
};

module.exports = search;
